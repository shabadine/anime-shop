<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Anime Goodies Shop{% endblock %}</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Bangers&family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
    
    <style>
        body {
            font-family: 'Roboto', sans-serif;
        }
        h1, h2, h3 {
            font-family: 'Bangers', cursive;
            color: #FF6B6B;
        }
        .navbar {
        background: linear-gradient(135deg, #e65c5c 0%, #e67e22 100%); 
    }
    /* liens blancs = OK, mais hover plus doux */
    .navbar-dark .navbar-nav .nav-link {
        color: rgba(255,255,255,.85);
        transition: color .2s;
    }
    .navbar-dark .navbar-nav .nav-link:hover,
    .navbar-dark .navbar-nav .nav-link:focus {
        color: #fff;
        text-shadow: 0 0 4px rgba(255,255,255,.45); */
    }
        .btn-primary {
            background-color: #FF6B6B;
            border-color: #FF6B6B;
        }
        .btn-primary:hover {
            background-color: #ff5555;
            border-color: #ff5555;
        }
        .btn-secondary {
            background-color: #4ECDC4;
            border-color: #4ECDC4;
        }
        .btn-secondary:hover {
            background-color: #45b8b0;
            border-color: #45b8b0;
        }
    </style>
    
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark">
    
        <div class="container">
            <img src="{{ asset('images/logo.svg') }}" alt="anime shop" width="49" height="40" loading="lazy">
            <a class="navbar-brand" href="{{ path('app_home') }}" style="font-family: 'Bangers', cursive; font-size: 1.8rem;">
                ANIME SHOP
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_catalog') }}">Catalogue</a>
                    </li>
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="#">Mon compte</a>
                        </li>
                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <a class="nav-link" href="#">Admin</a>
                            </li>
                        {% endif %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_cart_index') }}">
                         Panier ({{ cart_item_count() }})
                         

                         
                    </a>
                </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Flash messages -->
    {% for label, messages in app.flashes %}
        <div class="container mt-3">
            {% for message in messages %}
                <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}
        </div>
    {% endfor %}

    <!-- Content -->
    <main>
        {% block body %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white mt-5 py-4">
        <div class="container text-center">
        <!-- Logo -->
            <div class="col-md-2 text-center">
              <img src="{{ asset('images/logo.svg') }}" alt="anime shop" width="49" height="40" loading="lazy">
              <a class="navbar-brand" href="{{ path('app_home') }}" style="font-family: 'Bangers', cursive; font-size: 1.8rem;">
                ANIME SHOP
            </a>
            </div>
            <p>&copy; 2025 Anime Goodies Shop - Tous droits réservés</p>
            <p>Projet DWWM - Shabadine Bah</p>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>